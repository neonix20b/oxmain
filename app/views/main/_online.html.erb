<%= link_to("online:", {:controller=>'users',:action=>'live'},:class=>"linkfeed") if not @online.nil? and @online.size > 0%>
<%@online.each do |user|%>
  <%#= show_time(user.updated_at,"%H:%M:%S") %> 
  <%=profile_link(user)%>
<%end%>