<%= update_page_tag do |page|
	if not @comms.nil?
		@comms.each do |c|
			page.insert_html :bottom, 'all_comments', one_comment(c, c.profile);
			page.visual_effect :highlight, 'com_'+c.id.to_s, :duration => 8;
		end
	end
end%>